const myBookList = [];

function Book(title, author, pages, read="not read yet") {
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.read = read;
}

function addBookToList() {
  
}